name: IPTV Async Auto Tester Engine

on:
  schedule:
    - cron: '*/30 * * * *'  # প্রতি ৩০ মিনিট পর পর অটোমেটিক রান হবে
  workflow_dispatch:        # এর ফলে আপনি চাইলে যেকোনো সময় ম্যানুয়ালিও রান করতে পারবেন

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install aiohttp

    - name: Run IPTV Async Scraper & Tester
      run: python scraper.py

    - name: Commit and Push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add index.html live.m3u
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Live Channel Synced [Async Engine]" && git push)
      env:
        GITHUB_TOKEN: ${{ secrets.IPTV_TOKEN }}
